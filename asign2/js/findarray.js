var $ = function (id) {
  return document.getElementById(id);
};

function checkarray(h) {
      num.forEach(element => {
       if ( h == element )
             $("chodein").innerHTML = "Phan Tu nay da co trong mang"
      });
  }
  